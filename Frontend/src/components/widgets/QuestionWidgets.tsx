import { View, Text, FlatList } from "react-native";
import React from "react";
import { Quiz } from "../../utils";

export const QuestionWidgets = () => {
  // const renderQuestion: React.FC<{quiz: Quiz[]}> = ({quiz }) => {
  //   return (

  //   );
  // };

  return <View></View>;
};
